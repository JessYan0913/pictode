import Konva from 'konva';

export class Line extends Konva.Line {
  constructor(config: Konva.LineConfig) {
    super(config);
  }

  public draw(): this {
    return super.draw();
  }
}

export default Line;
